# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           @controllers.Application.home()
GET     /carousel                   @controllers.Application.carousel()
GET     /api/v1/centralStorage/      @controllers.Application.getCentralStorage()
GET     /api/v1/carousels             @controllers.Application.getCarouselList()
GET     /api/v1/countws             @controllers.Application.getWorkStationsCount()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file                controllers.Assets.at(path="/public", file)